export interface SeparatorProps {
  orientation?: 'horizontal' | 'vertical';
  /** Margin on both sides, px. */
  spacing?: number;
}
export declare function Separator(props: SeparatorProps): JSX.Element;
